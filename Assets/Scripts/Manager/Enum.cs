
public enum ListenType
{
    OnGameStart,
    OnGameOver,
    OnPlayerHeal,
    OnPlayerCollectItem,
    OnPauseGame,
    OnResumeGame,
    OnSendPlayerHeal,
    // Add more event types as needed
}
public enum UIType
{
    Unknow = 0,
    Screen = 1,
    Popup = 2,
    Notify = 3,
    Overlap = 4,
}